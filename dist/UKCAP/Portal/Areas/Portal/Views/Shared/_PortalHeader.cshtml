
<div class="navbar navbar-inverse">
    <div class="container">
        <div class="floatLeft">
            <span class="fa fa-search iconSearch"></span>
            <input class="search" type="search" name="search" placeholder="search...." />
        </div>
        <div class="navbar-collapse floatRight">
            <ul id="menu" class="nav navbar-nav">
                @*@if (Request.IsAuthenticated)
    {
        <li>@Html.ActionLink("Home_Authenticated", "Index", "Home")</li>
        <li>@Html.ActionLink("About_Authenticated", "About", "Home")</li>
        <li>@Html.ActionLink("Contact_Authenticated", "Contact", "Home")</li>
        var links = from item in menus
                        where item.Roles.Split(new String[] { "," }, StringSplitOptions.RemoveEmptyEntries)
                        .Any(x => roles.Contains(x) || x == "All")
                        select item;
            foreach (var link in links)
            {
                @:
                <li> @Html.ActionLink(link.LinkText, link.ActionName, link.ControllerName)</li>
            }
                }
                else*@
                @*{
    var links = from item in menus
                    where item.Roles.Split(new String[] { "," }, StringSplitOptions.RemoveEmptyEntries)
                    .Any(x => new String[] { "All", "Anonymous" }.Contains(x))
                    select item;
        foreach (var link in links)
        {
            @:
            <li> @Html.ActionLink(link.LinkText, link.ActionName, link.ControllerName)</li>
        }*@

                    <li class="mHide"><a href="#">A<sup>-</sup></a></li>
                    <li class="mHide"><a href="#">A</a></li>
                    <li class="mHide"><a href="#">A<sup>+</sup></a></li>
                    <li class="mHide"><a href="#">Hindi</a></li>
                    @*<li><a href="#"><i class="fa fa-sign-in"></i>Login</a></li>*@
                    <li>@Html.ActionLink("Login", "Login", "Account", new { area = "" }, null)</li>
                    @*<div class="dropdown">
            <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">
                Login
                <span class="caret"></span>
            </button>
            <ul class="dropdown-menu pull-left" style="float:right;">
                <li><a tabindex="-1" href="#">Kiosk Login</a></li>
                <li><a tabindex="-1" href="#">Desk 1 Login</a></li>
                <li><a tabindex="-1" href="#">Desk 2 Login</a></li>
                <li><a tabindex="-1" href="#">Desk 3 Login</a></li>
            </ul>
        </div>


    }*@
            </ul>

        </div>
    </div>
</div>
<div class="header">
    <div class="floatLeft">
        <a href="/">
            <div class="floatLeft"><img class="odisalogo" src="~/webapp/kiosk/Images/odisalogo_1.png" alt="" style="width:90px;margin-right:10px;" /><img src="~/PortalImages/depLgog.png" alt="" style="width:90px;margin-right:10px;" /></div>
            <div class="floatLeft">
                <div class="compheader">Lokaseba Adhikara</div>
                <div class="sub-title">Common Application Portal (CAP)</div>
                <div class="sub-sub-title">An Initiative of Odisha Government</div>
            </div>
        </a>
    </div>
    <div class="floatRight" style="width: 60%">
        <div class="menuNav">
            <ul>
                <li><a hrf="/"><span class="fa fa-home"></span>Home</a></li>
                <li><a hrf="/"><span class="fa fa-briefcase"></span>About Us</a></li>
                <li><a hrf="/"><span class="fa fa-building"></span>Departments</a></li>
                <li><a hrf="/"><span class="fa fa-users"></span>Citizen Services</a></li>
                <li><a hrf="/"><span class="fa fa-phone-square"></span>Contact Us</a></li>
            </ul>
        </div>
    </div>
</div>